---
description: Global rule for project
globs: *,**/*
---
# CORE:
- Use TypeScript for type safety
- You must generate UI/frontend in /ui folder and generate code backend/server in /server folder

# Server:
- API path with prefix 'api'
  
# UI:
- DO NOT change the logic in ui/index.html and ui/src/main.tsx.
- DO NOT use React components or hooks.
- Use Linear Interpolation when moving the camera and player positions.
- DO NOT use the room state to constantly update the player’s position.
- Alway use VITE_API_URL and VITE_COLYSEUS_SERVER_URL 
- Using shape instead of image